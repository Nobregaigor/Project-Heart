from enum import IntEnum, Enum


class SPK_SETS(Enum):
    
    GROUP = "group"
    NAME = "name"
    SUBSET = "subset"

    GROUP_NAME = "group_name"
    NAME_GROUP = "group_name"
    
    GROUP_SUBSET = "subset_name"
    SUBSET_GROUP = "subset_name"